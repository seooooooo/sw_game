using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class icecream_rest : MonoBehaviour
{
    public static int[] iceCreamrest = new int[8] { 3, 3, 3, 3, 3, 3, 3, 3 };
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
